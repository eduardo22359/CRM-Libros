@{ ViewData["Title"] = "Producto > Detalle"; }

@model Producto

<dl>
    <dt>Id</dt>
    <dd>@Model.Id</dd>

    <dt>Descripción</dt>
    <dd>@Model.Descripcion</dd>

    <dt>Precio</dt>
    <dd>@Model.Precio</dd>

    <dt>Cantidad</dt>
    <dd>@Model.Cantidad</dd>
</dl>